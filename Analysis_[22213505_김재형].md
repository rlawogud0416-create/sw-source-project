# 날씨 맞춤형 외출복 코디 추천 프로그램 WC

\- 2. Analysis -

**(22213505 김재형)**

---

## [ Revision history ]

| Revision date | Version # | Description | Author |
| :-----------: | :-------: | :--- | :----: |
|      2026.05.06        |   1.0.0   | 초안 작성 | 김재형 |
|      2026.05.07        |   1.0.1   | 유스케이스 다이어그램 추가 | 김재형 |
|      2026.05.07        |   1.0.2   | 클래스 다이어그램 추가 | 김재형 |

---

## [ Contents ]

1. Introduction
2. Use case analysis
3. Domain analysis
4. User Interface prototype
5. Glossary
6. References

---

## 1. Introduction

이 문서는 'WC(Weather Coordinator) - 날씨 맞춤형 외출복 코디 추천 프로그램'의 분석 단계 산출물이다. 앞선 Conceptualization에서 잡은 큰 그림을 바탕으로, 시스템이 어떤 기능을 가져야 하는지를 유스케이스 관점에서 들여다 보고, 어떤 클래스들이 필요한지를 정리한다. 또 사용자가 실제로 마주하게 될 CLI 화면이 어떤 모습일지도 미리 그려본다.

### 1.1 Project features

**(1) 유용성**

매일 아침 옷을 고르느라 애먹는 시간을 줄여주는 게 이 프로그램의 핵심이다. 옷장을 한 번만 등록해 두면, 그 다음부턴 외출할 시각의 기온만 입력해도 알아서 적당한 코디 한 세트를 골라준다. 기온이 들쭉날쭉한 환절기에 특히 도움이 된다.

**(2) 중요성**

요즘 코디 추천 앱들은 옷 사진을 일일이 찍어서 올려야 하는 게 보통이다. 그게 아니면 쇼핑몰에서 신상품을 사라고 권하는 마케팅 도구거나. 이 프로그램은 그런 무거운 기능 없이, 그냥 텍스트로 옷 정보만 넣어두면 된다. 옷의 두께를 1~5단계로 수치화해서 기온과 매칭시키는 방식이라 가볍게 돌아간다. 그리고 C++의 객체지향 기능(상속, 다형성, 캡슐화)을 제대로 써먹을 수 있다는 점도 의미가 있다.

**(3) 확장성**

지금은 옷의 두께만 가지고 추천하지만, 나중에 색상이나 스타일, 소재 같은 속성을 추가하면 더 똑똑한 추천이 가능하다. 또 현재는 사용자가 직접 기온을 입력해야 하지만, 추후에 기상 API를 연결하거나 GUI 버전으로 옮기는 것도 가능하도록 클래스를 분리해서 설계할 예정이다. 데이터를 저장하는 방식도 텍스트 파일로 시작하지만, DB로 갈아끼우기 쉽게 만들어 둔다.

---

## 2. Use case analysis

### 2.1 Use case diagram

<img width="471" height="620" alt="sdsd" src="https://github.com/user-attachments/assets/b1032715-5fd5-4701-8d3f-9945f50354b6" />


[그림 2-1] Use case diagram

### 2.2 Use case description

각 유스케이스에 대한 상세 설명은 아래와 같다.

#### UC-1) 옷장 데이터 로드

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-01 |
| Use case name | 옷장 데이터 로드 |
| Actor | System |
| Pre-condition | 사용자가 프로그램을 실행했고, 저장된 옷 데이터 파일이 있는 상태 |
| Main flow | 1. 프로그램이 시작된다.<br>2. 시스템이 실행 폴더에서 `wardrobe.txt`를 찾는다.<br>3. 파일을 한 줄씩 읽어 콤마(,)로 잘라낸다.<br>4. 잘라낸 값으로 카테고리에 맞는 옷 객체(Top, Bottom, Outer)를 만든다.<br>5. 만든 객체들을 Wardrobe 리스트에 담는다.<br>6. 메인 메뉴를 띄운다. |
| Alternative flow | 2-1. 파일이 없으면 빈 옷장으로 시작하고 안내 메시지를 띄운다.<br>3-1. 어딘가 깨진 줄이 있으면 그 줄은 건너뛴다. |
| Post-condition | 옷장 데이터가 메모리에 올라간 상태가 된다. |

#### UC-2) 신규 의류 등록

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-02 |
| Use case name | 신규 의류 등록 |
| Actor | User |
| Pre-condition | 사용자가 메인 메뉴에 있는 상태 |
| Main flow | 1. 사용자가 '1. 옷 추가' 메뉴를 고른다.<br>2. 카테고리(1.상의 / 2.하의 / 3.아우터)를 묻는 창이 나온다.<br>3. 사용자가 번호를 입력한다.<br>4. 옷 이름을 입력하라고 한다.<br>5. 사용자가 이름을 입력한다.<br>6. 두께 레벨(1~5)을 묻는다.<br>7. 사용자가 두께를 입력한다.<br>8. 시스템이 입력값을 확인하고 옷장에 추가한다.<br>9. `wardrobe.txt`에 바로 저장한다.<br>10. "등록 완료" 메시지를 보여주고 메인 메뉴로 돌아간다. |
| Alternative flow | 3-1. 카테고리 번호가 1-3이 아니면 오류 메시지 후 다시 입력받는다.<br>5-1. 옷 이름이 빈 문자열이면 오류 메시지 후 다시 입력받는다.<br>7-1. 두께 레벨이 1-5 외 값이거나 숫자가 아니면 오류 메시지 후 다시 입력받는다. |
| Post-condition | 새 의류가 옷장과 파일 양쪽에 추가된다. |

#### UC-3) 옷장 조회 / 필터링

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-03 |
| Use case name | 옷장 조회 / 필터링 |
| Actor | User |
| Pre-condition | 메인 메뉴에 있는 상태 |
| Main flow | 1. 사용자가 '2. 옷장 보기'를 고른다.<br>2. 필터 옵션(0.전체 / 1.상의 / 2.하의 / 3.아우터)이 뜬다.<br>3. 사용자가 번호를 고른다.<br>4. 조건에 맞는 옷들을 표 형태로 보여준다 (번호, 카테고리, 이름, 두께).<br>5. 아무 키나 누르면 메인 메뉴로 돌아간다. |
| Alternative flow | 4-1. 보여줄 옷이 없으면 "조회된 옷이 없습니다" 안내를 띄운다. |
| Post-condition | 선택한 조건의 옷 목록이 화면에 출력된다. |

#### UC-4) 의류 정보 수정

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-04 |
| Use case name | 의류 정보 수정 |
| Actor | User |
| Pre-condition | 옷장에 옷이 1개 이상 있는 상태 |
| Main flow | 1. 사용자가 '3. 옷 수정'을 고른다.<br>2. 시스템이 전체 옷 목록을 번호와 함께 보여준다.<br>3. 사용자가 수정할 옷의 번호(ID)를 친다.<br>4. 해당 옷의 현재 정보가 뜨고, 뭘 바꿀지(이름/두께) 묻는다.<br>5. 사용자가 새 값을 입력한다.<br>6. 시스템이 값을 확인하고 객체를 업데이트한다.<br>7. 파일에도 바로 반영한다. |
| Alternative flow | 3-1. 없는 번호면 안내 메시지 띄우고 메인 메뉴로 돌아간다.<br>5-1. 새 값이 이상하면 다시 입력받는다. |
| Post-condition | 옷 정보가 메모리와 파일 양쪽에서 갱신된다. |

#### UC-5) 의류 삭제

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-05 |
| Use case name | 의류 삭제 |
| Actor | User |
| Pre-condition | 옷장에 옷이 1개 이상 있는 상태 |
| Main flow | 1. 사용자가 '4. 옷 삭제'를 고른다.<br>2. 옷 목록을 번호와 함께 보여준다.<br>3. 사용자가 삭제할 번호를 친다.<br>4. "정말 삭제할까요? (Y/N)"이라고 한 번 더 묻는다.<br>5. 사용자가 'Y'를 누르면 옷장에서 객체를 빼고 메모리도 풀어준다.<br>6. 변경 내용을 파일에 저장한다. |
| Alternative flow | 3-1. 없는 번호면 안내 띄우고 메인 메뉴로 돌아간다.<br>5-1. 'N'을 누르면 작업이 취소된다. |
| Post-condition | 해당 옷이 옷장과 파일에서 사라진다. |

#### UC-6) 기온 입력 및 코디 추천

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-06 |
| Use case name | 기온 입력 및 코디 추천 |
| Actor | User |
| Pre-condition | 옷장에 상의·하의·아우터가 각각 1벌 이상 있는 상태 |
| Main flow | 1. 사용자가 '5. 코디 추천받기'를 고른다.<br>2. "오늘의 기온(℃)을 입력하세요" 라고 묻는다.<br>3. 사용자가 기온을 입력한다.<br>4. 시스템이 기온-두께 매핑 표를 보고 적정 두께를 정한다.<br>5. 그 두께에 맞는 옷들을 카테고리별로 골라낸다.<br>6. 각 카테고리에서 무작위로 1벌씩 뽑아 한 세트를 만든다.<br>7. 결과를 박스 형태로 보여준다. |
| Alternative flow | 3-1. 숫자가 아니거나 너무 이상한 값(-50 미만, 60 초과)이면 다시 묻는다.<br>5-1. 어느 한 카테고리라도 옷이 부족하면 "옷을 더 추가해주세요"라고 안내한다. |
| Post-condition | 기온에 맞는 코디 한 세트가 화면에 출력된다. |

#### UC-7) 추천 결과 재요청

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-07 |
| Use case name | 추천 결과 재요청 |
| Actor | User |
| Pre-condition | 추천을 한 번 받은 상태 |
| Main flow | 1. 추천 결과 화면 아래에 "다시 추천받을까요? (Y/N)"이 뜬다.<br>2. 사용자가 'Y'를 누른다.<br>3. 시스템이 같은 기온으로 다시 무작위로 뽑는다.<br>4. 가급적 직전과 다른 조합이 나오도록 결과를 보여준다. |
| Alternative flow | 2-1. 'N'을 누르면 메인 메뉴로 돌아간다.<br>3-1. 가능한 조합이 1개뿐이면 같은 결과가 나오고, 그 점을 안내한다. |
| Post-condition | 새 추천 결과가 화면에 뜬다. |

#### UC-8) 프로그램 종료

| 항목 | 내용 |
| :--- | :--- |
| Use case ID | UC-08 |
| Use case name | 프로그램 종료 |
| Actor | User, System |
| Pre-condition | 메인 메뉴에 있는 상태 |
| Main flow | 1. 사용자가 '0. 종료'를 고른다.<br>2. 시스템이 메모리의 옷장 데이터를 `wardrobe.txt`에 덮어쓴다.<br>3. 동적 할당했던 옷 객체들의 메모리를 풀어준다.<br>4. "프로그램을 종료합니다" 메시지를 띄우고 종료한다. |
| Alternative flow | 2-1. 파일 쓰기에 실패하면 경고만 띄우고 그래도 정상 종료한다. |
| Post-condition | 데이터가 파일에 안전하게 저장되고 프로그램이 끝난다. |

---

## 3. Domain analysis

이번 프로젝트에서 필요한 클래스들을 객체지향 관점에서 정리해 보았다.

### 3.1 Class diagram


<img width="594" height="557" alt="fddf" src="https://github.com/user-attachments/assets/38aa125d-5aa2-4bdb-b186-766b591e7c66" />

[그림 3-1] Class diagram

### 3.2 Class description

각 클래스의 역할을 하나씩 짚어본다.

#### (1) Clothing (의류)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | 모든 옷의 공통 속성과 동작을 정의하는 **추상 클래스**. |
| 주요 속성 | `id` (고유 번호), `name` (옷 이름), `thicknessLevel` (두께, 1~5) |
| 주요 메서드 | `getCategory()` — 순수 가상 함수, 자식이 구현<br>`display()` — 콘솔 출력<br>`toFileString()` — 파일에 저장할 문자열 반환 |
| 비고 | Top, Bottom, Outer가 이 클래스를 상속한다. `vector<Clothing*>`로 한꺼번에 관리할 수 있어서 다형성을 자연스럽게 활용할 수 있다. |

#### (2) Top (상의)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | Clothing을 상속해서 '상의'를 표현. |
| 비고 | `getCategory()`는 "Top"을 돌려준다. |

#### (3) Bottom (하의)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | Clothing을 상속해서 '하의'를 표현. |
| 비고 | `getCategory()`는 "Bottom"을 돌려준다. |

#### (4) Outer (아우터)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | Clothing을 상속해서 '아우터'를 표현. |
| 비고 | `getCategory()`는 "Outer"를 돌려준다. |

#### (5) Wardrobe (옷장)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | 등록된 모든 옷을 담아두는 컨테이너 클래스. |
| 주요 속성 | `clothingList` (`vector<Clothing*>`), `nextId` (다음에 줄 ID) |
| 주요 메서드 | `add()`, `remove()`, `update()`, `findById()`, `filterByCategory()`, `filterByThickness()`, `displayAll()` |
| 비고 | Clothing과 1:N 관계. 소멸자에서 자식 객체들을 모두 해제할 책임이 있다. |

#### (6) OutfitRecommender (코디 추천기)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | 입력된 기온을 보고 옷장에서 적당한 옷을 골라 한 세트로 묶어주는 핵심 로직 클래스. |
| 주요 메서드 | `tempToThickness(temp)` — 기온을 두께 레벨로 변환 (static)<br>`recommend(temp, wardrobe)` — 코디 한 세트 반환 |
| 비고 | Wardrobe를 사용해서(uses) 옷 데이터에 접근한다. |

#### (7) Outfit (코디)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | 추천된 한 세트(상의 + 하의 + 아우터)를 담는 데이터 객체. |
| 주요 속성 | `top`, `bottom`, `outer` (각 카테고리 옷의 포인터) |
| 주요 메서드 | `display()` — 코디 정보를 박스 형태로 콘솔에 출력<br>`isValid()` — 세 카테고리가 모두 채워졌는지 확인 |

#### (8) DataStorage (데이터 저장소)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | 옷장 데이터를 파일에 저장하고 불러오는 일을 맡는다. |
| 주요 메서드 | `save(wardrobe, path)` — `<fstream>`을 써서 텍스트 파일로 저장 (static)<br>`load(wardrobe, path)` — 파일을 읽어서 객체로 복원 (static) |
| 비고 | 카테고리 문자열을 보고 어떤 자식 객체(`new Top`, `new Bottom`, `new Outer`)를 만들지 정하는 일종의 팩토리 역할도 한다. 나중에 DB로 바꾸더라도 이 클래스만 손보면 된다. |

#### (9) CLIView (콘솔 화면)

| 항목 | 내용 |
| :--- | :--- |
| 역할 | 콘솔 입출력을 담당. 비즈니스 로직과 화면 출력을 분리하기 위해 따로 만든다. |
| 주요 메서드 | `showMainMenu()`, `showWardrobe()`, `showRecommend()`, `getInput()`, `showError()` |
| 비고 | 나중에 GUI로 갈아끼우게 되더라도 이 클래스만 갈아치우면 되도록 설계한다 (MVC 패턴의 View 역할). |

### 3.3 기온-두께 매핑 규칙

`OutfitRecommender::tempToThickness()`가 사용하는 기준은 아래와 같다.

| 기온 범위 (℃) | 적정 두께 | 설명 |
| :---: | :---: | :--- |
| 28 이상 | 1 | 한여름 (반팔, 얇은 면 등) |
| 23 ~ 27 | 2 | 늦여름·초가을 (얇은 긴팔, 가벼운 셔츠) |
| 17 ~ 22 | 3 | 환절기 (긴팔, 얇은 가디건) |
| 10 ~ 16 | 4 | 늦가을·초겨울 (니트, 자켓) |
| 9 이하 | 5 | 한겨울 (두꺼운 코트, 패딩) |

[표 3-1] 기온-두께 매핑 기준

### 3.4 데이터 파일 포맷

`wardrobe.txt`는 한 줄에 옷 한 벌씩 저장한다. 각 속성은 콤마(`,`)로 구분한다.

```
[id],[category],[name],[thicknessLevel]
```

예시:

```
1,Top,흰색 반팔 티셔츠,1
2,Top,회색 긴팔 티셔츠,3
3,Bottom,청바지,3
4,Bottom,검정 슬랙스,3
5,Outer,베이지 가디건,2
6,Outer,검정 패딩,5
```

[표 3-2] wardrobe.txt 데이터 포맷

---

## 4. User Interface prototype

본 프로그램은 CLI(Command Line Interface) 환경에서 돌아간다. 모든 입출력은 키보드와 콘솔 화면을 통해서만 일어난다. 각 화면이 어떤 모습일지 미리 그려보았다.

### 4.1 메인 메뉴 화면

```
============================================================
         WC - Weather Coordinator (v1.0)
            날씨 맞춤형 외출복 코디 추천
============================================================
  현재 등록된 옷: 6벌 (상의 2 / 하의 2 / 아우터 2)
------------------------------------------------------------
  [ 1 ] 옷 추가하기
  [ 2 ] 옷장 보기
  [ 3 ] 옷 정보 수정하기
  [ 4 ] 옷 삭제하기
  [ 5 ] 코디 추천받기
  [ 0 ] 프로그램 종료
============================================================
  메뉴를 선택하세요 >> _
```

[그림 4-1] 메인 메뉴 화면

**사용 방법**

(1) 화면 아래 프롬프트(`>>`)에 메뉴 번호(0~5)를 치고 Enter를 누르면 된다.

(2) 잘못된 번호나 글자가 들어오면 오류 메시지가 뜨고 다시 입력받는다.

(3) 화면 위쪽에 현재 옷장 상태가 한눈에 보이도록 요약해서 표시한다.

### 4.2 옷 추가 화면

```
============================================================
                     [ 옷 추가 ]
============================================================
  카테고리를 선택하세요:
    [ 1 ] 상의 (Top)
    [ 2 ] 하의 (Bottom)
    [ 3 ] 아우터 (Outer)
  선택 >> 1

  옷 이름을 입력하세요:
  >> 회색 긴팔 티셔츠

  두께 레벨을 입력하세요 (1: 가장 얇음 ~ 5: 가장 두꺼움):
  >> 3

------------------------------------------------------------
  ✓ 등록 완료!
    [#7] 상의 / 회색 긴팔 티셔츠 / Lv.3
------------------------------------------------------------
  Enter 키를 눌러 메인 메뉴로 돌아가세요...
```

[그림 4-2] 옷 추가 화면

**사용 방법**

(1) 메인 메뉴에서 `1`을 누르면 이 화면으로 들어온다.

(2) 카테고리 → 이름 → 두께 순서로 차례차례 묻는다.

(3) 입력값이 이상하면 그 단계에서 다시 묻는다.

(4) 등록이 끝나면 곧바로 `wardrobe.txt`에 저장된다.

### 4.3 옷장 보기 화면

```
============================================================
                    [ 내 옷장 ]
============================================================
  필터를 선택하세요:
    [ 0 ] 전체 보기  [ 1 ] 상의  [ 2 ] 하의  [ 3 ] 아우터
  선택 >> 0

------------------------------------------------------------
   ID  | 카테고리 | 옷 이름                  | 두께
------------------------------------------------------------
   01  | 상의      | 흰색 반팔 티셔츠         | Lv.1
   02  | 상의      | 회색 긴팔 티셔츠         | Lv.3
   03  | 하의      | 청바지                   | Lv.3
   04  | 하의      | 검정 슬랙스              | Lv.3
   05  | 아우터    | 베이지 가디건            | Lv.2
   06  | 아우터    | 검정 패딩                | Lv.5
------------------------------------------------------------
  총 6벌
============================================================
  Enter 키를 눌러 메인 메뉴로 돌아가세요...
```

[그림 4-3] 옷장 보기 화면

**사용 방법**

(1) 메인 메뉴에서 `2`를 누르면 이 화면으로 들어온다.

(2) 필터 번호를 누르면 그 카테고리만 골라서 보여준다.

(3) 옷 정보는 ID, 카테고리, 이름, 두께 순으로 표처럼 정리해서 출력한다.

### 4.4 코디 추천 화면

```
============================================================
                  [ 코디 추천받기 ]
============================================================
  오늘의 기온(℃)을 입력하세요:
  >> 15

  ★ 기온 15℃ → 권장 두께 레벨: 4 (늦가을·초겨울) ★

╔══════════════════════════════════════════════════════════╗
║                  오늘의 추천 코디                         ║
╠══════════════════════════════════════════════════════════╣
║  👕 상의   :  회색 긴팔 티셔츠         (Lv.3)             ║
║  👖 하의   :  검정 슬랙스              (Lv.3)             ║
║  🧥 아우터 :  베이지 가디건            (Lv.2)             ║
╚══════════════════════════════════════════════════════════╝

  다시 추천받으시겠습니까? (Y/N) >> _
```

[그림 4-4] 코디 추천 화면

**사용 방법**

(1) 메인 메뉴에서 `5`를 누르면 이 화면으로 들어온다.

(2) 기온을 입력하면 시스템이 권장 두께를 계산해서 보여준다.

(3) 그 아래로 추천 결과가 박스 형태로 뜬다.

(4) `Y`를 누르면 같은 기온으로 다른 조합을 다시 받을 수 있다.

(5) `N`을 누르면 메인 메뉴로 돌아간다.

### 4.5 옷 삭제 확인 화면

```
============================================================
                     [ 옷 삭제 ]
============================================================
  삭제할 옷의 번호(ID)를 입력하세요:
  >> 5

  삭제 대상: [#5] 아우터 / 베이지 가디건 / Lv.2

  정말 삭제하시겠습니까? (Y/N) >> y

------------------------------------------------------------
  ✓ 삭제 완료!
------------------------------------------------------------
  Enter 키를 눌러 메인 메뉴로 돌아가세요...
```

[그림 4-5] 옷 삭제 확인 화면

**사용 방법**

(1) 메인 메뉴에서 `4`를 누르면 이 화면으로 들어온다.

(2) 삭제할 옷의 번호를 입력하면 어떤 옷을 지울지 한 번 보여준다.

(3) 실수로 지우는 일이 없도록 `Y/N`으로 한 번 더 묻고, 그 다음에 삭제한다.

---

## 5. Glossary

| 용어 | 설명 |
| :--- | :--- |
| WC | 본 프로젝트의 이름. Weather Coordinator의 줄임말이다. |
| CLI | Command Line Interface의 약자. 마우스 없이 키보드로 텍스트만 가지고 컴퓨터와 대화하는 방식이다. 본 프로그램이 돌아가는 환경이다. |
| 사용자 | 프로그램을 실행해서 옷장 등록과 코디 추천을 사용하는 사람을 말한다. |
| 카테고리 | 옷의 종류를 가리킨다. 본 시스템에서는 상의(Top), 하의(Bottom), 아우터(Outer) 세 가지로 나눈다. |
| 두께 레벨 | 옷의 두께와 계절감을 숫자로 바꾼 것. 1단계(여름)부터 5단계(한겨울)까지 5단계로 나뉜다. |
| 코디 | 상의 1벌 + 하의 1벌 + 아우터 1벌이 모인 한 세트의 옷 조합을 말한다. |
| Wardrobe | 등록된 모든 옷 객체를 담아두는 컨테이너 클래스. |
| 다형성 | 부모 타입(Clothing) 포인터로 자식 객체(Top, Bottom, Outer)를 통째로 다룰 수 있게 해주는 객체지향의 특성이다. |
| 직렬화 | 메모리에 있는 객체를 파일에 저장하기 위해 문자열(`id,category,name,thickness` 형태)로 바꾸는 작업이다. |
| 역직렬화 | 반대로, 파일에 저장된 문자열을 다시 메모리 객체로 되살리는 작업이다. |
| 추상 클래스 | 순수 가상 함수가 1개 이상 들어 있어서 인스턴스를 직접 만들 수 없는 클래스. 본 시스템의 Clothing이 여기 해당한다. |
| 동적 할당 | `new` 키워드를 써서 힙 메모리에 객체를 만드는 방식. 다 쓰고 나면 `delete`로 꼭 풀어줘야 한다. |
| 텍스트 파일 입출력 | C++ 표준 라이브러리 `<fstream>`의 `ifstream`, `ofstream`을 통해 텍스트 파일을 읽고 쓰는 기능을 말한다. |
| 유스케이스 | 시스템이 사용자에게 어떤 기능을 어떻게 제공하는지를, 사용자(Actor)와 시스템 사이의 상호작용으로 표현한 것이다. |

---

## 6. References

\- 핀터레스트 이미지: https://kr.pinterest.com/pin/643803709227427094/

\- Stroustrup, B. (2013). *The C++ Programming Language* (4th ed.). Addison-Wesley.

\- Larman, C. (2004). *Applying UML and Patterns: An Introduction to Object-Oriented Analysis and Design and Iterative Development* (3rd ed.). Prentice Hall.

\- C++ Reference - `<fstream>`: https://en.cppreference.com/w/cpp/header/fstream
